class Solution {
    public boolean containsDuplicate(int[] n) {
        Arrays.sort(n);
        for(int a=0;a<n.length-1;a++){
            if(n[a]==n[a+1]){
                return true;
            }
        }
        return false;
    }
}
